name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t uyen/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true uyen/react-test npm test

      - run: docker build -t uyen/multi-client ./client
      - run: docker build -t uyen/multi-nginx ./nginx
      - run: docker build -t uyen/multi-server ./server
      - run: docker build -t uyen/multi-worker ./worker

      - run: docker push uyen/multi-client
      - run: docker push uyen/multi-nginx
      - run: docker push uyen/multi-server
      - run: docker push uyen/multi-worker
